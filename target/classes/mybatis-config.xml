<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
  
<configuration>
	<properties resource="driver.properties"/>
	<settings>
		<setting name="jdbcTypeForNull" value="NULL"/>
	</settings>
	<typeAliases>
		<typeAlias type="com.pps.ek.noticeBoard.model.vo.NoticeBoard" alias="NoticeBoard"/>
	</typeAliases>
	<environments default="DEV03">
			<environment id="DEV03">
				<!-- 
					트랜잭션을 제어하기 위한 설정
					
					type 속성
						JDBC : JDBC가 commit과 rollback을 직접 사용 가능하게 하는 옵션
						MANAGED : 트랜잭션에 대해 어떠한 직접적인 영향도 행사하지 않는 옵션
				-->
				<transactionManager type="JDBC" />
	
				<!-- 
					실제 DB 접속에 관한 정보를 넣는 태그
					
					type 속성
						POOLED : 커넥션 객체를 미리 만들어서 POOL에 저장하여 사용하는 설정, 객체를 만드는데 시간을 줄일 수 있다.
						
						UNPOOLED : DB 요청이 올때마다 커넥션 객체를 만들고 사용이 끝나면 닫는 설정 미리 객체를 만들어놓는 설정이 아니므로 상대적으로 느릴 수 있다. 
				 -->
				<dataSource type="POOLED">
					<property name="driver" value="${db.driver}"/>
					<property name="url" value="${db.url}"/>
					<property name="username" value="${db.username}"/>
					<property name="password" value="${db.password}"/>
				</dataSource>
			</environment>
		</environments>
	
		<!-- DB에 사용되는 쿼리문들을 담는 mapper 파일을 등록하는 부분 -->
		<mappers>
			<mapper resource="mappers/noticeBoard/NoticeBoardMapper.xml" />
		</mappers>
</configuration>